var array = ["bob", "jimmy", "max b", "bernie", "jordan", "future hendrix"];
public boolean isEvens (int[] array){
    for (int i = 0; i < array.length; i++) {
        if (array[i] % 2 != 0) {
            return false;
        } 
    }
    return true;
}

for (var i=0; i < arraylength; i++)
var newArray = arraylength.map(function(x) 
{return {1,2,6 };})